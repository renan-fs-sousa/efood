class MenuProducts {
  id: number
  title: string
  image: string
  text: string

  constructor(id: number, title: string, image: string, text: string) {
    this.id = id
    this.title = title
    this.image = image
    this.text = text
  }
}

export default MenuProducts
